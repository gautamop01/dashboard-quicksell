import React from "react";

// CustomSpinner component is used to display the spinner when the data is loading
const CustomSpinner = () => {
  return (
    <div className="spinner-backdrop">
      <div className="spinner"></div>
    </div>
  );
};

export default CustomSpinner;
